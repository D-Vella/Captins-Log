services:
  app:
    build: .
    ports:
      - "8501:8501"
    depends_on:
      - ollama
    volumes:
      - ./data:/app/data
      - ./db:/app/db

  ollama:
    image: ollama/ollama
    volumes:
      - ollama_models:/root/.ollama

volumes:
  ollama_models: